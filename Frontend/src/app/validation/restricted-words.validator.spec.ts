import { RestrictedWords } from './restricted-words.validator';

describe('RestrictedWords', () => {
  it('should create an instance', () => {
    expect(new RestrictedWords()).toBeTruthy();
  });
});
